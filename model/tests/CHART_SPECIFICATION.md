# Задача: Визуализация свечного графика опционов и греков (Strike Chart)

## 🎯 Цель
Реализовать профессиональный финансовый график для анализа опционов со следующими характеристиками: **свечной график цены опциона** в верхней части и **синхронизированные подграфики греков** под ним.

---

## 📊 Состав визуализации

### 1. **Главный график (Option Price Board)**
- **Тип**: Candlestick (Свечи OHLC).
- **Данные**: Open, High, Low, Close цен опциона.
- **Период**: 180-240 торговых дней.
- **Второй Overlay (Ось Y2)**: Линия спот-цены базового актива (BTC/ETH).
- **Стилизация**:
  - Зелёные свечи: `#76D7C4` (Call акцент).
  - Красные свечи: `#FF8787` (Put акцент).
  - Пунктирная серая линия для спота: `#969696`.

### 2. **Подграфики греков (Greeks Subplots)**
Каждый грек отображается на отдельной панели под основным графиком:

| Параметр | Описание | Цвет | Примечание |
|:---|:---|:---|:---|
| **IV** | Implied Volatility (%) | `#9B59B6` | Диапазон 30-100% |
| **Theta** | Time Decay ($) | `#E67E22` | Обычно отрицательные значения |
| **Delta** | Sensitivity | `#3498DB` | Диапазон 0.0 - 1.0 |
| **Gamma** | Convexity | `#F1C40F` | Малые дробные значения |
| **Vega** | Volatility sensitivity | `#1ABC9C` | Денежное выражение |

---

## ✅ Техническая спецификация (Мельчайшие подробности)

### 1. **Интерактивность и Синхронизация**
- **Crosshair Sync**: При наведении курсора на ЛЮБОЙ график (главный или любой грек), вертикальная линия Crosshair должна отображаться на ВСЕХ графиках одновременно в той же временной точке.
- **Legend Sync**: Легенды на всех панелях должны динамически обновляться, показывая точные значения (OHLC для свечей, числовые значения для греков и спота) в точке наведения курсора.
- **Time Axis Sync**: Масштабирование (Zoom) и прокрутка (Pan) по оси X должны быть жестко синхронизированы между всеми компонентами.
- **Alignment**: Левые и правые границы областей рисования должны быть СТРОГО выровнены по вертикали. Это требует фиксированной или синхронизированной ширины областей меток осей Y (Y-Axis tick areas).
- **Auto-scaling**: При перемещении по графику (Pan) оси Y должны автоматически подстраиваться под видимый диапазон данных.

### 2. **Динамическое управление Layout**
- **Toggle-кнопки**: Возможность включать/выключать отображение каждого грека по отдельности.
- **Пропорции высоты**:
  - **Только цена**: 100% высоты.
  - **Динамическое расширение области греков**: Каждый новый активный грек забирает у основного графика пространство, равное половине предыдущего "прироста":
    - 1 грек: Основной 75%, Область греков 25% (Прирост 25%).
    - 2 грека: Основной 62.5%, Область греков 37.5% (Прирост 25/2 = 12.5%).
    - 3 грека: Основной 56.25%, Область греков 43.75% (Прирост 12.5/2 = 6.25%).
    - 4 грека: Основной 53.125%, Область греков 46.875% (Прирост 6.25/2 = 3.125%).
    - 5 греков: Основной 51.5625%, Область греков 48.4375% (Прирост 3.125/2 = 1.5625%).
  - **Разделение области греков**: Вся "Область греков" делится СТРОГО ПОРОВНУ между всеми включенными на данный момент греками.
- **Плавность**: Изменение размеров панелей при переключении должно быть визуально плавным (без резких скачков верстки).
- **Ось времени**: Временная шкала (даты) отображается ТОЛЬКО на самом нижнем активном графике. На промежуточных графиках ось X скрыта.
- **Единая область**: Минимальные отступы между панелями, чтобы график выглядел как единая аналитическая доска.

### 3. **Визуальный дизайн (Trading UI)**
- **Фон**: Строго белый (`#FFFFFF`).
- **Сетка**: Тонкая, светло-серая (`rgba(200, 200, 200, 0.15)`).
- **Линии текущей цены (Price Lines)**:
  - На каждом графике должна быть горизонтальная пунктирная линия, соответствующая последнему значению данных.
  - Справа от линии (на оси или рядом) должна быть метка (Label) с числовым значением последней цены в цветной рамке, соответствующей цвету графика.
- **Аннотации**: Отображение меток текущей цены для опциона (справа) и спота (слева на Y2).
- **Легенды**: Располагаются в верхней части каждой панели (желательно в правом верхнем углу внутри области графика).

### 4. **Производительность**
- Плавный скролл и зум без лагов (целевое время отклика < 50ms).
- Быстрая первая отрисовка (цель < 500ms).
- Моментальная реакция на переключение (Toggle) греков.

---

## 📦 Референсная структура данных

### **OHLC Данные (Option)**
```python
df_ohlc = pd.DataFrame({
    'timestamp': [datetime, ...],  # 180-240 записей
    'open': [float, ...],
    'high': [float, ...],
    'low': [float, ...],
    'close': [float, ...],
})
```

### **Данные Спота**
```python
df_spot = pd.DataFrame({
    'timestamp': [datetime, ...],
    'value': [float, ...],  # Цена BTC/ETH
})
```

### **Данные Греков (Словарь)**
```python
df_greeks = {
    'iv': pd.DataFrame({'timestamp': [...], 'value': [...]}),      # в %
    'theta': pd.DataFrame({'timestamp': [...], 'value': [...]}),   # в $
    'delta': pd.DataFrame({'timestamp': [...], 'value': [...]}),   # 0-1
    'gamma': pd.DataFrame({'timestamp': [...], 'value': [...]}),   # малые числа
    'vega': pd.DataFrame({'timestamp': [...], 'value': [...]}),    # в $
}
```

---

## 🎨 Визуальный макет (Схематично)

```
┌─────────────────────────────────────────────────────────────┐
│ BTC $55000 CALL - 31 Aug 2024 (Option Analysis)             │
├─────────────────────────────────────────────────────────────┤
│                                 [Legend: O:1200 H:1255...]  │
│       ┌─────┐                                    [Label]───┤←Опцион (Y1)
│       │ ↑ │ │     ╱╲                                        │
│   ┌───┘   └─┴────╱  ╲─────  ← Spot (dashed gray)            │
│───┴──────────────────────────────────────────[Label]────────┤←Спот (Y2)
├─────────────────────────────────────────────────────────────┤
│ IV (%): 75.32                                     [Label]───┤
│     ╱╲    ╱╲                                                 │
│ ───╱  ╲──╱  ╲─────                                           │
├─────────────────────────────────────────────────────────────┤
│ Theta ($): -25.67                                 [Label]───┤
│ ─────╲  ╱────╲                                               │
│       ╲╱      ╲                                              │
└─────────────────────────────────────────────────────────────┘
     [Янв] [Фев] [Мар] [Апр] [Май] [Июн]
```
